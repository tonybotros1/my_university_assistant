// This screen is for checking for the user if he signed in or not (if signed in go to the app directly if not go to login screen)

import 'package:flutter/material.dart';

class LoadingScreen extends StatelessWidget {
  const LoadingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}